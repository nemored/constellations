query User {
  user {
    id
    username
    bookmarks {
      id
      url
      description
      categories {
        id
        name
      }
    }
    categories {
      id
      name
      bookmarks {
        id
        url
        description
        categories {
          id
          name
        }
      }
    }
  }
}
